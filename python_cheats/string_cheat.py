class String:
    def __init__(self, text: str):
        self.text = text

    def count(self, substring: string) -> int:
        return "Number of substring in self.text"

    def split(self, demiliter: str, split_count) -> list[str]:
        return "Where to cun and cut how many times"